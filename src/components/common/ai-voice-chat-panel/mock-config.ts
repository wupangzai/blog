/**
 * 面板默认配置。
 * 用于控制流式显示速度、mock 返回延迟以及录音弹窗里的波形条数量。
 */
export const defaultPanelConfig = {
  streamSpeed: 36,
  mockDelay: 1400,
  recordingBars: 24,
};

/**
 * 对话面板初始展示的欢迎语。
 * 组件挂载后会作为第一条服务端消息显示在列表顶部。
 */
export const initialAssistantMessage =
  '你好，我已经准备好了。你可以直接输入，或者点击录音按钮开始一次 mock 语音对话。';

/**
 * mock 语音对话种子数据。
 * 每次模拟语音请求时，会随机取一条作为用户转译文本和服务端回复内容。
 */
export const mockConversationSeeds = [
  {
    userText: '帮我整理今天会议的三个重点，并提醒我下午两点回电话。',
    assistantText:
      '今天会议的三个重点是需求确认、排期同步和风险跟踪。下午两点需要回电给客户，我已经帮你整理成待办提醒。',
  },
  {
    userText: '请根据刚才的语音，生成一段适合发给客户的礼貌确认消息。',
    assistantText:
      '可以发送给客户的确认消息是：您好，刚刚沟通的内容我已确认，后续会按约定时间推进，有进展会第一时间同步给您。',
  },
  {
    userText: '我想快速了解这个页面的交互流程，并给我一个优化建议。',
    assistantText:
      '当前流程可以概括为语音输入、转译确认、服务响应和结果操作。建议把录音结束后的反馈再做得更即时，比如增加一个短暂的状态过渡。',
  },
];

/**
 * 文字输入场景下的固定 mock 回复。
 * 用来模拟服务端一次性返回结果后的文本展示效果。
 */
export const typedInputMockReply =
  '我已经收到你的文字输入。这是一个 mock 返回结果，用来模拟服务端一次性返回文本和音频后的界面表现。';
